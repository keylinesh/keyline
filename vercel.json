{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "pnpm -r build && mkdir -p public && cp index.html favicon.svg og-image.png og-image.svg robots.txt sitemap.xml public/",
  "outputDirectory": "public"
}
