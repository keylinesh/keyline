{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "pnpm -r build && mkdir -p public && cp index.html favicon.svg og-image.png og-image.svg robots.txt sitemap.xml install.sh public/ && cp demo/keyline-demo.mp4 public/demo.mp4 && cp demo/keyline-demo.webm public/demo.webm && cp -r apps/web/dist public/app",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/[[...route]]" },
    { "source": "/install", "destination": "/install.sh" },
    { "source": "/app/:path*", "destination": "/app/index.html" }
  ],
  "headers": [
    {
      "source": "/install",
      "headers": [{ "key": "Content-Type", "value": "text/plain; charset=utf-8" }]
    }
  ]
}
